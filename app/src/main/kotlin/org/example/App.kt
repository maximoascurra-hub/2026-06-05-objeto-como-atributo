/*
 * This source file was generated by the Gradle 'init' task
 */
package org.example
import org.example.nose.Persona
import org.example.nose.Fecha
import org.example.nose.Polinomio

import org.example.nose.Termino
fun main() {
    println(Persona("pepe", "honguito", Fecha(5, 6, 2026)))

    var lista = ArrayList <Polinomio>()

    // lista.add(Cuadratica())
    // lista.add(Polinomio())
    // lista.add(CuadraticaMejor())
    // lista.add(Cuatrinomio())
    // println("desde acá es la lista")
    // for (polinomio in lista){
    // println(polinomio)
    // }

    var t1=Termino(2,3)
    var t2=Termino(3,2)
    var t3=Termino(99,0)
    var pol1=Polinomio(listOf(t1,t2,t3))
    var t4=Termino(1,1)
    var polinomio2=Polinomio(listOf(t1,t2,t4,t3))
    println(pol1)
    println(polinomio2)
}